import product from './product' 
import blockContent from './blockContent'
import category from './category'
import newPrduct from './newProduct'

export const schemaTypes = [product, blockContent, category, newPrduct]
